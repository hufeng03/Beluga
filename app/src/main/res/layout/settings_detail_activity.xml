<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/windows"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:background="@color/settings_bg"
    android:layout_height="fill_parent">
    <!--RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/top_tab_bg">
        <LinearLayout 
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:orientation="horizontal"
            android:layout_centerInParent="true">
			<TextView
				android:id="@+id/settings_title"
				android:layout_width="wrap_content"
	            android:layout_height="wrap_content"
	            android:textAppearance="@style/Large_Title"
				android:text="@string/settings_title"/>
        </LinearLayout>
        <Button 
        	android:id="@+id/back" 
        	android:layout_centerVertical="true" 
        	android:layout_alignParentLeft="true" 
        	android:background="@drawable/btn_back_bg" 
        	android:layout_width="80dip" 
        	android:layout_height="wrap_content" 
        	android:text="@string/btn_back_default" 
        	android:textColor="@color/white"
        	android:paddingLeft="15dip"/>
    </RelativeLayout-->
    
    <ListView 
        android:id="@+id/settings_detail_list"
	    android:cacheColorHint="@null"
	    android:divider="@drawable/list_seperator"
	    android:fastScrollEnabled="true"
        android:layout_height="0.0dip"
        android:layout_width="fill_parent"
        android:layout_weight="1.0"
        />

        <EditText
            android:id="@+id/settings_detail_edit"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            />
            <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        >
                <Button
            android:id="@+id/settings_detail_recover"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_weight="1"
            android:text="@string/settings_detail_recover"
            />
        <Button
            android:id="@+id/settings_detail_add"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_weight="1"
            android:text="@string/settings_detail_add"
            />
        </LinearLayout>
    
</LinearLayout>